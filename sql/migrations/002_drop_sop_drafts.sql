-- ---------------------------------------------------------------------
-- 002_drop_sop_drafts.sql — 안 쓰는 "서버 임시 저장" 표를 없앱니다.
--
-- 왜 없애나요?
--   편집기의 "이 기기에 임시 저장" 은 브라우저 안(localStorage)에만 저장하는 기능이라
--   이 표를 한 번도 쓰지 않았습니다. 쓰지 않는 표는 두면 헷갈리기만 해서 정리합니다.
--
-- 이미 없는 DB 에서 실행해도 안전합니다(IF EXISTS). 여러 번 실행해도 결과는 같습니다.
-- ---------------------------------------------------------------------

DROP TABLE IF EXISTS sop_drafts;
